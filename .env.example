# Copy this file to .env and fill in real values. .env is gitignored and
# must never be committed. No client-side code (site/) ever reads this --
# odds are fetched server-side by src/data_collection/collect_odds.py and
# only the resulting CSV/JSON (no key) is committed or deployed.

# The Odds API (https://the-odds-api.com/) -- free tier: 500 credits/month,
# 1 credit per region per market. Used for EPL 1X2 (h2h) odds.
ODDS_API_KEY=
